{
	"name": "virtual-venue-server",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "node server.js",
		"deploy": "gcloud app deploy app.yaml --project virtual-venue -q",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^4.17.1",
		"meow": "^8.0.0",
		"simple-signal-server": "^3.0.0",
		"socket.io": "^3.1.0",
		"twilio": "^3.55.0"
	},
	"devDependencies": {
		"dotenv": "^8.2.0"
	}
}
